# yolox-x-detection-pipeline

DIMER inference and bounded fine-tuning pipeline for YOLOX-X.

The pipeline, its tests, the model card and the standalone tutorial land via the initial build pull request.
